[
  {
    "name": "Now Playing asks for a location",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Share your location to find movies nearby, or type a city, ZIP, or two-letter country code."
            }
          }
        ],
        "send": {
          "callback": "now_playing:start"
        }
      }
    ]
  }
  ,{
    "name": "Now Playing validates a manual location",
    "steps": [
      { "send": { "callback": "now_playing:start" }, "expect": [{ "method": "sendMessage" }] },
      { "send": { "text": "x" }, "expect": [{ "method": "sendMessage", "payload": { "text": "That doesn't look like a city or ZIP. Try again with a nearby place." } }] }
    ]
  }
]
